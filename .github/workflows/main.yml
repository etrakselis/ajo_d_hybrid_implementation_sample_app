name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v4

      - name: Build or deploy
        run: |
          cd $GITHUB_WORKSPACE
          # Now you're in a git repo
          git status
